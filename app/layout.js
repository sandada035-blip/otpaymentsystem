import "./globals.css"
export const metadata = { title: "School System Pro Login Roles", description: "Admin full access and User view only" }
export default function RootLayout({ children }) {
  return <html lang="km"><body>{children}</body></html>
}
